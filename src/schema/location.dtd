<!ENTITY % elements SYSTEM "elements.dtd">
%elements;
<!ENTITY % actions SYSTEM "game-data.dtd">
%actions;

<!ELEMENT game-location (
        img, game-data, load+
        )>
<!ELEMENT load EMPTY>

<!ATTLIST game-location
        id     ID        #REQUIRED
        class  PCDATA    #FIXED "scene"
        music  NMTOKENS  #REQUIRED
        >

<!ATTLIST load
        src    PCDATA    #REQUIRED
        style  PCDATA    #IMPLIED
        >